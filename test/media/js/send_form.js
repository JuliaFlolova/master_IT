$(document).ready(function(){			$('#send_mail').submit(registerUser); //При нажатии на кнопку "Отправить на форме" 		});	 	function registerUser(event)			{				event.preventDefault();				var form_data = $('#send_mail').serialize(); //собираем значения с формы методом serialize 						//Begin the ajax call				$.ajax({						url: send_url, // путь до метода обработки ajax. переменная задана в файле шаблона header_view.php						type: "POST",      						data: form_data,										dataType: "html",					  					 success: function (json) {                                                              $('#message').html(json).show();																			  }				});			}